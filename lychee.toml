# What the link check does not check, and why.
#
# Nothing is excluded. The waitlist form is gone (the page collects nothing), so
# the reserved-name endpoint went with it, and every link left on the page is
# expected to resolve and is checked. The page's home is
# `https://selvage-protocol.vercel.app`: the canonical link, `og:url` and
# `og:image` all name that origin, and it resolves, so there is nothing to
# exclude. `selvageprotocol.com` is not registered and registering it is not
# being pursued. If a URL ever needs an exclusion, add it here with its reason
# beside it.
